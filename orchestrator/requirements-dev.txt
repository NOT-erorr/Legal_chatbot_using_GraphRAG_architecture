pytest>=8.3.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# ── RAGAS evaluation harness (orchestrator/tests/eval) ──────────────────────
# Pin coherent langchain 0.3.x line: ragas hard-imports
# `langchain_community.chat_models.vertexai`, có trong 0.3.x nhưng đã bị bỏ ở
# langchain-community >= 0.4. Bộ pin này vẫn thoả requirements.txt của orchestrator
# (langgraph>=0.4, langchain-core>=0.3). Khuyến nghị cài trong venv riêng.
ragas>=0.2.14,<0.3
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-google-genai>=2.0,<3.0
langgraph>=0.4,<0.5
datasets>=2.0
