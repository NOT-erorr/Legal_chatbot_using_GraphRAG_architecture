# ============================================================================
# Orchestrator Service (LangGraph) — Dependencies
# ============================================================================

# ---- LangGraph Core ----
# Chặn trần để prod == bản đã test (langgraph 0.4.x / langchain-core 0.3.x).
# langgraph 1.x / langchain-core 1.x chưa được kiểm thử với code này.
langgraph>=0.4.0,<0.5.0
langchain-core>=0.3.0,<0.4.0

# ---- LLM Provider ----
google-genai>=1.0.0

# ---- Vector + Graph DB Clients ----
qdrant-client>=1.9.0
neo4j>=5.25.0

# ---- PostgreSQL ----
psycopg2-binary>=2.9.9

# ---- Redis ----
redis>=5.1.0

# ---- Web Framework ----
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
pydantic-settings>=2.5.0

# ---- Auth / Password hashing ----
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1,<4.1   # passlib 1.7.4 không tương thích bcrypt>=4.1 (warning __about__)
google-auth>=2.30.0  # xác minh Google ID token cho /api/v1/auth/google
PyJWT>=2.8.0         # phát/giải mã access token (JWT) cho các endpoint bảo vệ

# ---- Utils ----
python-dotenv>=1.0.0
